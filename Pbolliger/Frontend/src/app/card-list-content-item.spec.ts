import { CardListContentItem } from './card-list-content-item';

describe('CardListContentItem', () => {
  it('should create an instance', () => {
    expect(new CardListContentItem()).toBeTruthy();
  });
});
